/*Ext.define('EmailClient.Application', {
    name: 'EmailClient',
    extend: 'Ext.app.Application',

    views: [
        // TODO: add views here
    ],

    controllers: [
             //'EmailClient.controller.Email',
             //'EmailClient.controller.ToDo',
             //'EmailClient.controller.Header'
    ],

    stores: [
             //'Owners', 'ToDos'
    ]
});
*/